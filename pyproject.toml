[tool.poetry]
name = "bookstore"
version = "0.1.0"
description = ""
authors = ["lcpires7"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.13"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
